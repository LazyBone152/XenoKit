<UserControl x:Class="XenoKit.Views.BSA.BsaType7View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:XenoKit.Views.BSA"
             xmlns:bsaDic="clr-namespace:Xv2CoreLib.ValuesDictionary;assembly=Xv2CoreLib"
             d:DataContext="{d:DesignInstance Type=local:BsaType7View}"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="520">
    <Grid>
        <StackPanel>
            <Expander Header="Sound" IsExpanded="True">
                <WrapPanel>
                    <Grid Margin="3,5,0,0" Width="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="ACB File" ToolTip="Which ACB file the cue is played from."/>
                        <ComboBox Grid.Column="1" ItemsSource="{x:Static bsaDic:BSA.AcbType}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType7View}}, Path=BsaViewModel.AcbType}"/>
                    </Grid>
                    <Grid Margin="3,5,0,0" Width="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Cue ID" ToolTip="ID of the cue in the selected ACB file. 65535 means no sound."/>
                        <controls:NumericUpDown Grid.Column="1" Minimum="0" Maximum="65535" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType7View}}, Path=BsaViewModel.CueId, UpdateSourceTrigger=Default}"/>
                        <Button Grid.Column="2" Margin="4,0,0,0" Width="24" Height="24" Padding="0" Click="SoundPreview_Click" ToolTip="Preview this cue"
                                Style="{StaticResource MahApps.Styles.Button.Circle}">
                            <iconPacks:PackIconMaterialLight Kind="Play" Width="10" Height="10"/>
                        </Button>
                    </Grid>
                </WrapPanel>
            </Expander>

            <Expander Header="Unknown" IsExpanded="False">
                <WrapPanel>
                    <Grid Margin="3,5,0,0" Width="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="I_02"/>
                        <controls:NumericUpDown Grid.Column="1" Minimum="0" Maximum="65535" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType7View}}, Path=BsaViewModel.I_02, UpdateSourceTrigger=Default}"/>
                    </Grid>
                    <Grid Margin="3,5,0,0" Width="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="I_06"/>
                        <controls:NumericUpDown Grid.Column="1" Minimum="0" Maximum="65535" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType7View}}, Path=BsaViewModel.I_06, UpdateSourceTrigger=Default}"/>
                    </Grid>
                </WrapPanel>
            </Expander>
        </StackPanel>
    </Grid>
</UserControl>
