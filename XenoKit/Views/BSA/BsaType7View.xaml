<UserControl x:Class="XenoKit.Views.BSA.BsaType7View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="760">
    <Grid>
        <Expander Header="Sound" IsExpanded="True">
            <WrapPanel>
                <Grid Margin="3,5,0,0" Width="250">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="ACB File"/>
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding AcbTypes}"
                              SelectedItem="{Binding AcbType, UpdateSourceTrigger=Default}"/>
                </Grid>
                <Grid Margin="3,5,0,0" Width="300" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="125"/>
                        <ColumnDefinition Width="125"/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Cue ID"/>
                    <controls:NumericUpDown Grid.Column="1"
                                            Value="{Binding CueId, UpdateSourceTrigger=Default}"
                                            Maximum="65535"
                                            Minimum="0"/>
                    <Button Grid.Column="2"
                            Click="SoundPreview_Click"
                            Width="22"
                            Height="22"
                            Style="{DynamicResource MahApps.Styles.Button.Circle}">
                        <iconPacks:PackIconMaterialLight Kind="Play" Width="15" Height="15"/>
                    </Button>
                </Grid>
            </WrapPanel>
        </Expander>
    </Grid>
</UserControl>
