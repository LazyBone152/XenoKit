<UserControl x:Class="XenoKit.Views.BSA.BsaTypeBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:XenoKit.Views.BSA"
             d:DataContext="{d:DesignInstance Type=local:BsaTypeBaseView}"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="520">
    <Grid>
        <Expander Header="Activation" IsExpanded="True">
            <WrapPanel>
                <Grid Margin="3,5,0,0" Width="250">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Start Time" ToolTip="The frame this subtype activates on, relative to the projectile spawning."/>
                    <controls:NumericUpDown Grid.Column="1" Minimum="0" Maximum="65535" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaTypeBaseView}}, Path=BsaViewModel.StartTime, UpdateSourceTrigger=Default}"/>
                </Grid>
                <Grid Margin="3,5,0,0" Width="250">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Duration" ToolTip="How many frames this subtype stays active for."/>
                    <controls:NumericUpDown Grid.Column="1" Minimum="0" Maximum="65535" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaTypeBaseView}}, Path=BsaViewModel.Duration, UpdateSourceTrigger=Default}"/>
                </Grid>
            </WrapPanel>
        </Expander>
    </Grid>
</UserControl>
