<UserControl x:Class="XenoKit.Views.BSA.BsaType12View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:XenoKit.Views.BSA"
             xmlns:bsaDic="clr-namespace:Xv2CoreLib.ValuesDictionary;assembly=Xv2CoreLib"
             d:DataContext="{d:DesignInstance Type=local:BsaType12View}"
             mc:Ignorable="d"
             d:DesignHeight="250" d:DesignWidth="520">
    <Grid>
        <StackPanel>
            <Expander Header="Skill Reference" IsExpanded="True">
                <WrapPanel>
                    <Grid Margin="3,5,0,0" Width="360">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="175"/>
                            <ColumnDefinition Width="175"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Skill Type" TextWrapping="Wrap" VerticalAlignment="Center" Margin="3,0,5,0" ToolTip="Skill category used with the Skill ID to find the BSA resource whose Entry Passing entries receive this signal."/>
                        <ComboBox Grid.Column="1" ItemsSource="{x:Static bsaDic:BSA.EepkType}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType12View}}, Path=BsaViewModel.SkillType}"/>
                    </Grid>
                    <Grid Margin="3,5,0,0" Width="360">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="175"/>
                            <ColumnDefinition Width="175"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Skill ID" TextWrapping="Wrap" VerticalAlignment="Center" Margin="3,0,5,0" ToolTip="Skill ID paired with Skill Type when the game looks up the BSA resource whose Entry Passing entries receive this signal."/>
                        <controls:NumericUpDown Grid.Column="1" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType12View}}, Path=BsaViewModel.SkillID, UpdateSourceTrigger=Default}"/>
                    </Grid>
                </WrapPanel>
            </Expander>

            <Expander Header="Entry Passing Signal" IsExpanded="True">
                <WrapPanel>
                    <Grid Margin="3,5,0,0" Width="360">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="175"/>
                            <ColumnDefinition Width="175"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Signal Value" TextWrapping="Wrap" VerticalAlignment="Center" Margin="3,0,5,0" ToolTip="Signal sent to BSA Entry Passing entries (Type 0). When value matching is enabled, the Entry Passing entry compares this with its condition value."/>
                        <controls:NumericUpDown Grid.Column="1" StringFormat="0.00" Interval="0.05" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType12View}}, Path=BsaViewModel.SignalValue, UpdateSourceTrigger=Default}"/>
                    </Grid>
                    <Grid Margin="3,5,0,0" Width="360">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="175"/>
                            <ColumnDefinition Width="175"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Delivery Mode" TextWrapping="Wrap" VerticalAlignment="Center" Margin="3,0,5,0" ToolTip="Controls how the signal reaches BSA Entry Passing entries. Broadcast sends to every matching entry. Same-context delivery sends only to the highest-priority matching entry."/>
                        <ComboBox Grid.Column="1" ItemsSource="{x:Static bsaDic:BSA.SignalDeliveryMode}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType12View}}, Path=BsaViewModel.DeliveryMode}"/>
                    </Grid>
                    <Grid Margin="3,5,0,0" Width="360">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="175"/>
                            <ColumnDefinition Width="175"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Pause Recipient Timeline (One Update)" TextWrapping="Wrap" VerticalAlignment="Center" Margin="3,0,5,0" ToolTip="A positive value pauses the receiving Entry Passing entry's timeline for one update."/>
                        <controls:NumericUpDown Grid.Column="1" StringFormat="0.00" Interval="0.05" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:BsaType12View}}, Path=BsaViewModel.PauseRecipientTimeline, UpdateSourceTrigger=Default}"/>
                    </Grid>
                </WrapPanel>
            </Expander>
        </StackPanel>
    </Grid>
</UserControl>
