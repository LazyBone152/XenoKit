<UserControl x:Class="XenoKit.Views.BSA.BsaType0View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="180" d:DesignWidth="760">
    <UserControl.Resources>
        <Style x:Key="BsaEditorField" TargetType="Grid">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style x:Key="BsaEditorLabel" TargetType="Label">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,0"/>
        </Style>
    </UserControl.Resources>

    <Expander Header="Pass BSA Entry" IsExpanded="True" Margin="0,2,0,0">
        <WrapPanel Margin="0,5">
            <Grid Style="{StaticResource BsaEditorField}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource BsaEditorLabel}" Content="BSA Entry"/>
                <controls:NumericUpDown Grid.Column="1" Value="{Binding BsaEntryId, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
            </Grid>
            <Grid Style="{StaticResource BsaEditorField}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource BsaEditorLabel}" Content="Main Condition"/>
                <controls:NumericUpDown Grid.Column="1" Value="{Binding MainCondition, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
            </Grid>
            <Grid Style="{StaticResource BsaEditorField}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource BsaEditorLabel}" Content="BAC Condition"/>
                <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding BacCondition, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
            </Grid>
            <Grid Style="{StaticResource BsaEditorField}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource BsaEditorLabel}" Content="I_00"/>
                <controls:NumericUpDown Grid.Column="1" Value="{Binding I_00, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
            </Grid>
            <Grid Style="{StaticResource BsaEditorField}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource BsaEditorLabel}" Content="I_06"/>
                <controls:NumericUpDown Grid.Column="1" Value="{Binding I_06, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
            </Grid>
            <Grid Style="{StaticResource BsaEditorField}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource BsaEditorLabel}" Content="F_12"/>
                <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding F_12, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
            </Grid>
        </WrapPanel>
    </Expander>
</UserControl>
