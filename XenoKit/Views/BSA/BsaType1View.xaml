<UserControl x:Class="XenoKit.Views.BSA.BsaType1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="260" d:DesignWidth="760">
    <UserControl.Resources>
        <Style x:Key="BsaEditorField" TargetType="Grid">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style x:Key="BsaEditorLabel" TargetType="Label">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,0"/>
        </Style>
        <Style x:Key="BsaFlagGroup" TargetType="GroupBox">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
    </UserControl.Resources>

    <Expander Header="Movement" IsExpanded="True" Margin="0,2,0,0">
        <StackPanel Margin="0,5">
            <WrapPanel>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Speed X"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding SpeedX, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Speed Y"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding SpeedY, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Speed Z"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding SpeedZ, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Acceleration X"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding AccelerationX, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Acceleration Y"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding AccelerationY, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Acceleration Z"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding AccelerationZ, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Falloff Strength"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding FalloffStrength, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Spread X"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding SpreadX, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Spread Y"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding SpreadY, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Spread Z"/>
                    <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Value="{Binding SpreadZ, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
            </WrapPanel>
            <TextBlock Margin="6,10,0,2" Text="Motion Flags"/>
            <ItemsControl ItemsSource="{Binding MotionFlagGroups}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <GroupBox Style="{StaticResource BsaFlagGroup}" Header="{Binding Name}">
                            <ItemsControl ItemsSource="{Binding Options}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </GroupBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Expander>
</UserControl>
