<UserControl x:Class="XenoKit.Views.BSA.BsaType3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="260" d:DesignWidth="760">
    <UserControl.Resources>
        <Style x:Key="BsaEditorField" TargetType="Grid">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style x:Key="BsaEditorLabel" TargetType="Label">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,0"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Expander Header="Hitbox Bounds" IsExpanded="True" Margin="0,2,0,0">
            <StackPanel Margin="0,5" HorizontalAlignment="Left">
                <WrapPanel>
                    <Grid Margin="3,5,0,0" Width="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Style="{StaticResource BsaEditorLabel}" Content="Type"
                               ToolTip="The bounding box type determines how the min and max values work. Uniform uses hitbox scale. MinMax uses min and max values plus hitbox scale."/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding BoundingBoxTypes}" SelectedItem="{Binding BoundingBoxType, UpdateSourceTrigger=Default}"/>
                    </Grid>
                    <Grid Margin="3,5,0,0" Width="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Style="{StaticResource BsaEditorLabel}" Content="Size" ToolTip="Minimum size of the hitbox on all axes."/>
                        <controls:NumericUpDown Grid.Column="1" StringFormat="0.00#####" Interval="0.05" Minimum="0.05" Maximum="100" Value="{Binding Size, UpdateSourceTrigger=Default}"/>
                    </Grid>
                    <Grid Margin="3,5,0,0" Width="250" IsEnabled="{Binding BoundsEnabled}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Style="{StaticResource BsaEditorLabel}"
                               Content="Grow Max Bounds"
                               ToolTip="When enabled, the preview extends max bounds by the projectile movement sweep."/>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding Switches}"
                                  SelectedItem="{Binding GrowMaxBounds, UpdateSourceTrigger=Default}"/>
                    </Grid>
                </WrapPanel>
                <Grid Margin="3,5,0,0" Width="500">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="105"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Position" ToolTip="Position of the hitbox relative to the projectile."/>
                    <Label Grid.Column="1" Style="{StaticResource BsaEditorLabel}" Content="X"/>
                    <controls:NumericUpDown Grid.Column="2" StringFormat="0.00" Interval="0.05" Value="{Binding PositionX, UpdateSourceTrigger=Default}"/>
                    <Label Grid.Column="3" Style="{StaticResource BsaEditorLabel}" Content="Y"/>
                    <controls:NumericUpDown Grid.Column="4" StringFormat="0.00" Interval="0.05" Value="{Binding PositionY, UpdateSourceTrigger=Default}"/>
                    <Label Grid.Column="5" Style="{StaticResource BsaEditorLabel}" Content="Z"/>
                    <controls:NumericUpDown Grid.Column="6" StringFormat="0.00" Interval="0.05" Value="{Binding PositionZ, UpdateSourceTrigger=Default}"/>
                </Grid>
                <Grid Margin="3,5,0,0" Width="500" IsEnabled="{Binding BoundsEnabled}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="105"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Min Bounds" ToolTip="The minimum bounds of the box."/>
                    <Label Grid.Column="1" Style="{StaticResource BsaEditorLabel}" Content="X"/>
                    <controls:NumericUpDown Grid.Column="2" StringFormat="0.00" Interval="0.05" Value="{Binding MinX, UpdateSourceTrigger=Default}"/>
                    <Label Grid.Column="3" Style="{StaticResource BsaEditorLabel}" Content="Y"/>
                    <controls:NumericUpDown Grid.Column="4" StringFormat="0.00" Interval="0.05" Value="{Binding MinY, UpdateSourceTrigger=Default}"/>
                    <Label Grid.Column="5" Style="{StaticResource BsaEditorLabel}" Content="Z"/>
                    <controls:NumericUpDown Grid.Column="6" StringFormat="0.00" Interval="0.05" Value="{Binding MinZ, UpdateSourceTrigger=Default}"/>
                </Grid>
                <Grid Margin="3,5,0,0" Width="500" IsEnabled="{Binding BoundsEnabled}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="105"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Max Bounds" ToolTip="The maximum bounds of the box."/>
                    <Label Grid.Column="1" Style="{StaticResource BsaEditorLabel}" Content="X"/>
                    <controls:NumericUpDown Grid.Column="2" StringFormat="0.00" Interval="0.05" Value="{Binding MaxX, UpdateSourceTrigger=Default}"/>
                    <Label Grid.Column="3" Style="{StaticResource BsaEditorLabel}" Content="Y"/>
                    <controls:NumericUpDown Grid.Column="4" StringFormat="0.00" Interval="0.05" Value="{Binding MaxY, UpdateSourceTrigger=Default}"/>
                    <Label Grid.Column="5" Style="{StaticResource BsaEditorLabel}" Content="Z"/>
                    <controls:NumericUpDown Grid.Column="6" StringFormat="0.00" Interval="0.05" Value="{Binding MaxZ, UpdateSourceTrigger=Default}"/>
                </Grid>
            </StackPanel>
        </Expander>

        <Expander Header="BDM IDs" IsExpanded="True" Margin="0,2,0,0">
            <WrapPanel Margin="0,5">
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="BDM First Hit ID"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding FirstHit, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="BDM Multiple Hits ID"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding MultipleHits, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="BDM Last Hit ID"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding LastHit, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
            </WrapPanel>
        </Expander>
    </StackPanel>
</UserControl>
