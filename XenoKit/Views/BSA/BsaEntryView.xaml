<UserControl x:Class="XenoKit.Views.BSA.BsaEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="360" d:DesignWidth="760">
    <UserControl.Resources>
        <Style x:Key="BsaEditorField" TargetType="Grid">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style x:Key="BsaEditorLabel" TargetType="Label">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,0"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Expander Header="Projectile" IsExpanded="True" Margin="0,2,0,0">
            <WrapPanel Margin="0,5">
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="ID"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding SortID, UpdateSourceTrigger=Default}" Minimum="0" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="I_00"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_00, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
            </WrapPanel>
        </Expander>

        <Expander Header="Impact Properties" IsExpanded="True" Margin="0,2,0,0">
            <WrapPanel Margin="0,5">
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Impact A"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_16_a, UpdateSourceTrigger=Default}" Minimum="0" Maximum="255" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Impact B"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_16_b, UpdateSourceTrigger=Default}" Minimum="0" Maximum="255" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="I_17"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_17, UpdateSourceTrigger=Default}" Minimum="0" Maximum="255" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="I_18"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_18, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
            </WrapPanel>
        </Expander>

        <Expander Header="Lifetime" IsExpanded="True" Margin="0,2,0,0">
            <WrapPanel Margin="0,5">
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Lifetime"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding Lifetime, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="I_24"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_24, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
            </WrapPanel>
        </Expander>

        <Expander Header="Pass On" IsExpanded="True" Margin="0,2,0,0">
            <WrapPanel Margin="0,5">
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Expires"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding Expires, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Impact Projectile"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding ImpactProjectile, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Impact Enemy"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding ImpactEnemy, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="Impact Ground"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding ImpactGround, UpdateSourceTrigger=Default}" Minimum="0" Maximum="65535" HideUpDownButtons="True"/>
                </Grid>
            </WrapPanel>
        </Expander>

        <Expander Header="Unknown" IsExpanded="False" Margin="0,2,0,0">
            <WrapPanel Margin="0,5">
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="I_40[0]"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_40_0, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="I_40[1]"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_40_1, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
                <Grid Style="{StaticResource BsaEditorField}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Style="{StaticResource BsaEditorLabel}" Content="I_40[2]"/>
                    <controls:NumericUpDown Grid.Column="1" Value="{Binding I_40_2, UpdateSourceTrigger=Default}" HideUpDownButtons="True"/>
                </Grid>
            </WrapPanel>
        </Expander>
    </StackPanel>
</UserControl>
