<Application x:Class="XenoKit.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:XenoKit" StartupUri="MainWindow.xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d1p1:Ignorable="d" xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" xmlns:converters="clr-namespace:LB_Common.Converters;assembly=LB_Common" xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks" DispatcherUnhandledException="Application_DispatcherUnhandledException" xmlns:d1p1="http://schemas.openxmlformats.org/markup-compatibility/2006" Startup="Application_Startup">
  <Application.Resources>
    <ResourceDictionary>
      <converters:VisibilityToNullableBooleanConverter x:Key="visibilityConverter" />
      <converters:InverseVisibilityConverter x:Key="inverseVisibilityConverter" />
      <converters:UInt16ToDoubleConverter x:Key="uint16Converter" />
      <BitmapImage x:Key="gridlines" UriSource="Resources/transparent_checkerboard.png" />
      <Style x:Key="UnderlinedTabControl" TargetType="controls:MetroTabControl">
        <Setter Property="controls:TabControlHelper.UnderlinePlacement" Value="Top" />
        <Setter Property="controls:TabControlHelper.Underlined" Value="SelectedTabItem" />
        <Setter Property="controls:TabControlHelper.UnderlineBrush" Value="{DynamicResource MahApps.Brushes.Accent}" />
        <Setter Property="controls:TabControlHelper.UnderlineMouseOverBrush" Value="Transparent" />
      </Style>
      <Style x:Key="ButtonNoGlow" TargetType="Button">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border Name="border" BorderThickness="0" BorderBrush="Black" Background="{TemplateBinding Background}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Bottom" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter Property="Opacity" Value="0.75" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="ExpandableDataGridHeaderStyle" TargetType="{x:Type DataGridRowHeader}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray5}" />
        <Setter Property="BorderThickness" Value="0 0 1 0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type DataGridRowHeader}">
              <Grid>
                <Grid HorizontalAlignment="Center" VerticalAlignment="Stretch" Margin="0,0,0,0" Background="{DynamicResource MahApps.Brushes.Accent}">
                  <ToggleButton x:Name="ExpanderToggleButton" Focusable="False" IsChecked="{Binding Path=DetailsVisibility,&#xD;&#xA;                                                    RelativeSource={RelativeSource AncestorType=DataGridRow},&#xD;&#xA;                                                    Converter={StaticResource visibilityConverter},&#xD;&#xA;                                                    Mode=TwoWay}" Style="{StaticResource {x:Static ToolBar.ToggleButtonStyleKey}}" Foreground="Transparent" Background="Transparent" BorderBrush="Transparent" Width="{Binding Path=RowHeaderWidth, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                </Grid>
                <Grid HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0">
                  <iconPacks:PackIconMaterialLight Kind="ArrowDown" Width="12" Height="12" VerticalAlignment="Center" Visibility="{Binding Path=DetailsVisibility, &#xD;&#xA;                                                     RelativeSource={RelativeSource AncestorType=DataGridRow},&#xD;&#xA;                                                     Converter={StaticResource inverseVisibilityConverter}}" HorizontalAlignment="Center" />
                  <iconPacks:PackIconMaterialLight Kind="ArrowUp" Width="12" Height="12" VerticalAlignment="Center" Visibility="{Binding Path=DetailsVisibility, &#xD;&#xA;                                                     RelativeSource={RelativeSource AncestorType=DataGridRow}}" HorizontalAlignment="Center" />
                </Grid>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="ExpandableDataGridHeaderStyle2" TargetType="{x:Type DataGridRowHeader}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray5}" />
        <Setter Property="BorderThickness" Value="0 0 1 0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type DataGridRowHeader}">
              <Grid>
                <Grid HorizontalAlignment="Center" VerticalAlignment="Stretch" Margin="0,0,0,0" Background="{DynamicResource MahApps.Brushes.Accent}">
                  <ToggleButton x:Name="ExpanderToggleButton" Focusable="False" IsChecked="{Binding DataContext.IsExpanded, &#xD;&#xA;                                                            RelativeSource={RelativeSource AncestorType=DataGridRow}, &#xD;&#xA;                                                            Mode=TwoWay}" Style="{StaticResource {x:Static ToolBar.ToggleButtonStyleKey}}" Foreground="Transparent" Background="Transparent" BorderBrush="Transparent" Width="{Binding Path=RowHeaderWidth, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                </Grid>
                <Grid HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0">
                  <iconPacks:PackIconMaterialLight Kind="ArrowDown" Width="12" Height="12" VerticalAlignment="Center" Visibility="{Binding Path=DetailsVisibility, &#xD;&#xA;                                         RelativeSource={RelativeSource AncestorType=DataGridRow},&#xD;&#xA;                                         Converter={StaticResource inverseVisibilityConverter}}" HorizontalAlignment="Center" />
                  <iconPacks:PackIconMaterialLight Kind="ArrowUp" Width="12" Height="12" VerticalAlignment="Center" Visibility="{Binding Path=DetailsVisibility, &#xD;&#xA;                                         RelativeSource={RelativeSource AncestorType=DataGridRow}}" HorizontalAlignment="Center" />
                </Grid>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml">
        </ResourceDictionary>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml">
        </ResourceDictionary>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml">
        </ResourceDictionary>
        <ResourceDictionary>
          <Style TargetType="{x:Type DataGrid}" BasedOn="{StaticResource {x:Type DataGrid}}">
            <Setter Property="controls:ControlsHelper.ContentCharacterCasing" Value="Normal" />
          </Style>
          <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="controls:ControlsHelper.ContentCharacterCasing" Value="Normal" />
          </Style>
          <Style TargetType="{x:Type Expander}" BasedOn="{StaticResource {x:Type Expander}}">
            <Setter Property="controls:ControlsHelper.ContentCharacterCasing" Value="Normal" />
          </Style>
        </ResourceDictionary>
        <ResourceDictionary>
          <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
            <Setter Property="FontSize" Value="11" />
          </Style>
          <vm:ViewModelLocator x:Key="Locator" d:IsDataSource="True" xmlns:vm="clr-namespace:XenoKit.ViewModel" />
        </ResourceDictionary>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Application.Resources>
</Application>